# MCP Hub Docker 环境配置
# 复制此文件为 .env 并根据需要修改

# ===========================================
# 基础配置
# ===========================================
PORT=3000
NODE_ENV=production

# ===========================================
# 数据库配置
# ===========================================
# PostgreSQL 配置
DB_PASSWORD=mcphub123
DB_PORT=5432
DB_HOST=postgres
DB_NAME=mcphub
DB_USER=mcphub

# Redis 配置
REDIS_PASSWORD=redis123
REDIS_PORT=6379
REDIS_HOST=redis

# ===========================================
# 安全配置 (生产环境必须修改!)
# ===========================================
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# 管理员密码配置（重要：生产环境必须设置！）
# 如果不设置，将使用默认密码 'mcphub@2024'
DEFAULT_ADMIN_PASSWORD=your-secure-admin-password

# ===========================================
# 可选配置
# ===========================================
# 代理设置（如果需要）
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NPM_REGISTRY=https://registry.npmjs.org/

# 性能设置
# REQUEST_TIMEOUT=60000

# 路径前缀（如果需要）
# BASE_PATH=/mcphub

# 功能开关
# INSTALL_EXT=false

# ===========================================
# Docker 配置
# ===========================================
COMPOSE_PROJECT_NAME=mcphub
RESTART_POLICY=unless-stopped